---
version: '3.8'

services:
  runner:
    image: roslynos-github-runner:latest
    build:
      context: .
      args:
        RUNNER_VERSION: '2.299.1'
    environment:
      GH_TOKEN: ${GH_TOKEN}
      GH_OWNER: ${GH_OWNER}
      GH_REPOSITORY: ${GH_REPOSITORY}